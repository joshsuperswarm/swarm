---
allowed-tools: Bash
description: Stage changes and create a commit (no push)
---

# Git Commit

## Current Status
!`git status`

## Changes to Commit
!`git diff`
!`git diff --cached`

## Recent Commits
!`git log --oneline -3`

## Task
1. Stage all changes with `git add .`
2. Create a well-formatted commit message based on the changes
3. Create the commit with Claude Code attribution
4. Do NOT push - just commit locally

Commit message context: $ARGUMENTS